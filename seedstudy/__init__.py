"""Seed study: how much of a generated artifact is determined by the seed alone?

Fix the prompt, sweep the seed, decompose the variance — identically for images
and for text, so the two modalities can be compared on the same footing.
"""

__version__ = "0.1.0"
